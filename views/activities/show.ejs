<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Activity <%= activities.title %></title>
  </head>
  <body>
    
    <h1><%= activities.title %></h1>
    <p><%= prompts.name %></p>
    <p><%= activities.note %></p>
    <p><%= activities.experienceDesc %></p>
    <img src="<%= activities.photo %>" alt="photo of <%= prompts.name %>"/>
    <div class="d-inline-flex m-2"></div>
      <form action="/activities/<%= activities.id %>/edit" method="GET">
        <button type="submit" class="btn btn-warning m-2">Edit</button>
      </form>
      <form action="/activities/<%= activities.id %>?_method=DELETE" method="POST">
        <button type="submit" class="btn btn-warning m-2">Delete Activity</button>
      </form>
    </div>
  </body>
</html>
  