<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Activity</title>
  </head>
  <body>
    <%-include ('../partials/loggedInLogo')%>
    <%-include ('../partials/loggedInNav')%>
    <h1>Edit or Delete Activity</h1>
    <p><%= prompts.name %></p>
    <section class="editActivityForm">
      <form action="/activities/<%= activities._id %>?_method=PUT" method="POST">
        <div>
          <label for="title">Title</label>
          <input type="text" name="title" />
        </div>
        <div>
          <label for="note">Note</label>
          <input type="text" name="note" />
        </div>
        <div>
          <label for="experienceDescription">Experience Description</label>
          <input type="text" name="experienceDescription" />
        </div>
        <div>
          <label for="photo">Photo</label>
          <input type="url" name="photo" />
        </div>
        <button type="submit">Edit Activity</button>
      </form>
      <form action="/activities/<%= activities._id %>?_method=DELETE" method="POST">
        <input type="submit" value="Delete Activity"/>
    </form>
    </section>
    <%-include ('../partials/loggedInFooter')%>
  </body>
</html>
  