<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Activity</title>
  </head>
  <body>
    <h1>Edit Activity</h1>
    <section class="editActivityForm">
      <form action="/activities/<%= activities._id %>?_method=PUT" method="POST">
        <div>
          <label for="title">Title</label>
          <input type="text" name="title" value="<%=activities.title%>" />
        </div>
        <div>
          <label for="note">Note</label>
          <input type="text" name="note" value="<%=activities.note%>" />
        </div>
        <div>
          <label for="experienceDesc">Experience Description</label>
          <input type="text" name="experienceDesc" value="<%=activities.experienceDesc%>"/>
        </div>
        <div>
          <label for="photo">Photo</label>
          <input type="url" name="photo" />
        </div>
        <button type="submit">Edit Activity</button>
      </form>
      <form action="/activities/<%= activities._id %>?_method=DELETE" method="POST">
        <input type="submit" value="Delete Activity"/>
    </form>
    </section>
  </body>
</html>
  