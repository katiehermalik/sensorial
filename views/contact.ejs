<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact Sensorial</title>
</head>
<body>
  <% if (user.isLoggedin === true) { %>
    <%-include ('partials/loggedInNav')%>
  <% } else { %>
    <%-include ('partials/loggedOutNav')%>
  <% } %> 
  <h1>Contact Us</h1>
  <h2>Hello <%= user.username %></h2>
  <form action="" method="" id="contact">
    <label for="name">Name</label>
    <input type="text" name="name">
    <label for="email">Email</label>
    <input type="text" name="email">
  </form>
  <textarea rows="4" cols="50" name="comment" form="contact">Your message here...</textarea>
  <input type="submit" value="Send" form="contact">
</body>
</html>